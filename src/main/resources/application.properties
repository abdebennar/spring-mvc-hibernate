# MySQL Database Configuration
db.url=jdbc:h2:file:./cinema_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=TRUE;MODE=MySQL
db.username=sa
db.password=
db.driver=org.h2.Driver
spring.jpa.hibernate.ddl-auto=update

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# File upload path
storage.path=/tmp/cinema-uploads

# # File upload path (for Exercise 02)
# storage.path=/tmp/cinema-uploads
# ```

# **Note:** `createDatabaseIfNotExist=true` will auto-create the `cinema` database if it doesn't exist.

# ---

# ## 🗂️ **4. Create Project Structure**
# ```
# Cinema/
# ├── src/main/
# │   ├── java/fr/cinema/
# │   │   ├── config/
# │   │   │   └── AppConfig.java
# │   │   ├── servlets/
# │   │   ├── repositories/
# │   │   └── filters/
# │   └── resources/
# │       ├── application.properties
# │       └── sql/
# │           ├── schema.sql    ← CREATE TABLES
# │           └── data.sql      ← INSERT TEST DATA
# └── pom.xml